# AudioCapture
A simple utility program build in C++ used to capture desktop audio, initially intended to be used in a Java subprocess, can also be used standalone - run the program in https://github.com/Mogrul/AudioCapture/releases/latest play some audio on your system and when you're done press enter in the console and a file called output.mp3 containing the captured desktop audio will be placed in the same directory as the program.

